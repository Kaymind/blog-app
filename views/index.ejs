<% include ./partials/header %>
    <h1>INDEX PAGE!!!</h1>

    <% blogs.forEach(function(blog){ %>
        <div>
            <div class="row">
                <h4><%= blog.title %></h4>
                <img src="<%= blog.image %>" alt="">
                <p><%= blog.created %></p>
                <p><%= blog.body %></p>
            </div>
            
        </div>
    <% }) %>
<% include ./partials/footer %>

